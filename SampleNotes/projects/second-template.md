# Secondary template

The loader searches subfolders recursively.

tags:
  - szablon
  - dokumentacja

This nested note verifies that shared tag counts include every Markdown file
below the selected folder.
